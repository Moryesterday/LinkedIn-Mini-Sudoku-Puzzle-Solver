# LinkedIn-Mini-Sudoku-Puzzle-Solver
Python solver for the Mini Sudoku puzzle on LinkedIn. Uses a built-from-scratch neural network to recognize digits from a screenshot and reconstruct the grid, applies a recursive backtracking algorithm to compute a valid solution, and automatically fills the grid via GUI automation.
